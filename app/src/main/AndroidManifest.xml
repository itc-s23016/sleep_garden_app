<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 前景サービス（アラーム鳴動で使用） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ で mediaPlayback を使う場合の宣言（推奨） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- 通知（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 正確なアラーム（Android 12+） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 鳴動中の WakeLock 用 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- フルスクリーン通知（推奨） -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- 画面オーバーレイに必要（設定で許可が必要） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sleep_Garden">

        <!-- 🌿 メイン画面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔔 アラーム停止UI（ロック画面でも前面表示） -->
        <activity
            android:name=".alarm.AlarmActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <!-- アラーム発火レシーバ -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- スヌーズレシーバ -->
        <receiver
            android:name=".alarm.SnoozeReceiver"
            android:exported="false" />

        <!-- 鳴動サービス（mediaPlayback の前景サービス） -->
        <service
            android:name=".alarm.AlarmRingtoneService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- 🌙 おやすみオーバーレイ（通常サービス。前景化しない） -->
        <service
            android:name=".sleep.SleepOverlayService"
            android:exported="false" />
    </application>
</manifest>
